import React                   from 'react';
import { Router,
         Route,
         BrowserHistory }      from 'react-router';
import App                     from 'app';

export default (
  <Route component={App} path="/" />
);
